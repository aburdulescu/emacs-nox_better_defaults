# emacs-nox_better_defaults
Better defaults for emacs-nox

## Install
```
git clone https://github.com/aburdulescu/emacs-nox_better_defaults.git
ln -s ~/emacs-nox_better_defaults/init.el ~/emacs.d/init.el
```

